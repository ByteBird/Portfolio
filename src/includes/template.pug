
block variables
    include config.pug
include mixins/mixin
<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title= `${maintitle} - ${currentPage}`
        link(rel="stylesheet", href="css/styles.css")
    body
        .container
            include header
            include main
            include footer